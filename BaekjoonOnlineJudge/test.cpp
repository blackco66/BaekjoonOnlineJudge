#include <iostream>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    char a[3], b[3], c[3];
    cin.getline(a, 3);
    cin.getline(b, 3);
    if (a == b) {
        cout << "aaaaaaaaa";
    }
    return 0;
}